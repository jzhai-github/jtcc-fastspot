{#
	{% include partial("link") with {
		title: "Button Text",
		url: "#",
		class: "",
		js: false,
		icon: "",
		aria: ""
	} %}
#}

<a href="{{ url ?: "#" }}" class="{{ class }}_link{% if js %} js-{{ class|replace({ "_": "-"}) }}-link{% endif %}" {% if aria %} aria-label="{{ aria }}" {% endif %}>
	<span class="{{ class }}_link_inner">
		<span class="{{ class }}_link_label">{{ title }}</span>
		{% if icon %}
			<span class="{{ class }}_link_icon" aria-hidden="true">
				<span class="{{ class }}_link_icon_inner">
					<span class="{{ class }}_link_icon_space">&#xfeff;</span>
					<svg class="icon icon_{{ icon }}">
						<use xlink:href="/images/icons.svg#{{ icon }}"></use>
					</svg>
				</span>
			</span>
		{% endif %}
	</span>
</a>
